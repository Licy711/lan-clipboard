# ============================================================
# Lan Clipboard 环境变量模板
# 使用方法：把本文件复制一份改名为 .env.local，然后把下面的值改成你自己的
# （.env.local 已被 .gitignore 忽略，不会上传到 GitHub）
# 如果部署到 Vercel，请在 Vercel 后台 → Project → Settings → Environment Variables
# 里按同样的变量名逐一添加（Production / Preview / Development 三个环境都勾上）
# ============================================================

# ---------------- 1. Redis（必选） ----------------
# 设备列表 + 房间消息的存储。
# 可以用：
#   - 自建 Redis（1Panel / Docker / 云服务器 + 密码）
#   - 或 Upstash Redis（推荐 Vercel 生态，serverless 友好）
#   - 或 Vercel KV（把下面这行替换成 VERCEL_KV_REST_API_URL 这种写法另改代码，本项目默认用 ioredis）
# 格式：redis://:密码@主机:端口
# 示例：redis://:your_redis_password@127.0.0.1:6379
REDIS_URL=

# ---------------- 2. Cloudflare R2 图床（必选，用来存聊天里的图片） ----------------
# 2.1 Cloudflare Account ID
# 查看位置：登录 Cloudflare → 进入任意 Zone → 右侧栏「Account ID」；
#          或 R2 桶页面 URL 里 /r2/ 前面那一段 32 位十六进制
# 示例：f5736c29d5fccde4284c001b0cd4b54b
R2_ACCOUNT_ID=

# 2.2 R2 API Token（创建位置：R2 → Manage R2 API Tokens → Create API Token）
# 权限必须勾选：【Object Read & Write】（不要勾 Admin 权限，最小权限原则）
# 作用范围：【只指定你要用的桶】（下面的 R2_BUCKET）
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=

# 2.3 存储桶名（Bucket），例如：lcv
R2_BUCKET=

# 2.4 桶的公网访问域名（末尾不带 /）
# 两种方式任选其一：
#   A. R2 桶 Settings → Public bucket access → Allow Access，系统给的 pub-xxx.r2.dev
#   B. （推荐）R2 桶 Settings → Custom Domains → 绑定你自己的子域名，走 Cloudflare CDN + 免费 SSL
R2_PUBLIC_DOMAIN=
